node_modules/
dist/
.astro/

# scripts/fetch-book.mjs writes these fresh on every build/dev run —
# nothing from upstream is vendored/committed. See README.md.
.book-src/
src/content/docs/book/
src/data/quizzes/
src/generated/

*.log
.DS_Store
.docker-home/

# scratch work dir for scripts/narrate.mjs — WAVs + timing files, before
# ffmpeg encodes them to the mp3s in public/audio/, which ARE committed
# (that's how the audio reaches Vercel — generation is a local step, not
# part of the Vercel build).
.tts/

.vercel
.env*
