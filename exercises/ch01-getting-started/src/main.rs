// ch01 starter — fill in the TODOs. `cargo run --bin starter` to try it,
// `cargo run --bin solution` to see a working version once you're stuck.

fn main() {
    // TODO 1: print your name and today's date on two separate lines,
    // using two separate println! calls.

    // TODO 2: print the same two lines using ONE println! call and two
    // `{}` placeholders instead. Store the name and date in variables
    // first with `let`.

    // TODO 3: cargo prints warnings for unused variables — silence today's
    // warnings on purpose by prefixing an unused variable name with `_`,
    // then remove the prefix once you actually use it above.
}
